if txt_encontrado is not None:
    txt_base = os.path.join(directorio_actual, txt_encontrado)
else:
    print("No se encontró ningún archivo que coincida con el prefijo y sufijo.")
    # Manejar el error o terminar la ejecución si es necesario

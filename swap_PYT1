from pathlib import Path

directorio_actual = Path(__file__).parent

archivos = list(directorio_actual.glob("NYL20*COLMASIVO.txt"))

if archivos:
    archivo_txt = archivos[0]
    print(f"Archivo encontrado: {archivo_txt}")
else:
    print("No se encontró ningún archivo que coincida con el patrón.")

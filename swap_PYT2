def obtener_campo(datos_linea, mapeo_campos, campo, valor_defecto=' '):
    """
    Función que obtiene un campo específico de una lista de datos según el mapeo dado.
    Si el campo no existe o está vacío, devuelve un valor por defecto.
    
    :param datos_linea: Lista con los datos de la línea del TXT.
    :param mapeo_campos: Diccionario con el mapeo de campos y sus posiciones.
    :param campo: Clave del campo a obtener del mapeo.
    :param valor_defecto: Valor a devolver si el campo está vacío o no existe (por defecto es un espacio).
    :return: Valor del campo o el valor por defecto.
    """
    indice = mapeo_campos.get(campo, None)
    if indice is not None and len(datos_linea) > indice and datos_linea[indice] != '':
        return datos_linea[indice]
    return valor_defecto

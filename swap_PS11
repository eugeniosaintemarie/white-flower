# Define la ubicación donde se guardará el archivo
$logFolder = "C:\Logs"
if (!(Test-Path -Path $logFolder)) {
    New-Item -ItemType Directory -Path $logFolder
}

# Genera el nombre del archivo con fecha y hora
$timestamp = (Get-Date).ToString("yyyy-MM-dd_HH-mm-ss")
$logFile = "$logFolder\Log_$timestamp.txt"

# Escribe la hora en que terminó el script dentro del archivo
$endTime = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
"El script finalizó a las: $endTime" | Out-File -FilePath $logFile -Encoding UTF8
